import { API_BASE_URL, postJson, fetchJson } from "./apiSettings";

export const getReports = async () => {
    // This will be the fetch for the data... once I figure out how to use 
    response = await fetch(``)
    return response.json()
}